pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,json}"
      run: pnpm lint
    typecheck:
      glob: "*.ts"
      run: pnpm typecheck

pre-push:
  parallel: true
  commands:
    test:
      run: pnpm test:run
